// In the asset pack’s build.gradle.kts file:
plugins {
    id("com.android.asset-pack")
}

assetPack {
    packName.set("app_assets") // Directory name for the asset pack
    dynamicDelivery {
        deliveryType.set("install-time")
    }
}
